export type Rect = {
  width: number;
  height: number;
  left: number;
  top: number;
};

export type Pos = { x: number; y: number };

export interface HwcEvent {
  startDate: Date;
  endDate: Date;
}
